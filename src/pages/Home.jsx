import { useEffect, useNavigate } from "react";


const Home = () => {
  // const navigate = useNavigate();

  useEffect(() => {
    // if (localStorage.getItem('user') === null) {
    //   navigate('/login');
    // }
  }, []);

  return <div>
    
  </div>;
};

export default Home;
